


show running programs with the specific process-name.
top -p `pgrep process-name | tr "\\n" "," | sed 's/,$//'`

top -p `pgrep qtcreator | tr "\\n" "," | sed 's/,$//'`

Find files.
find . -type f -name "*.o"

Delete files.
find . -type f -name "*.o" -delete

Find in directory, ignore binary files, ignore case, recursive, print line #, exclude certain directories, E=extended regular expression.
Find two words within 5 spaces of each other.
grep -ERIin --exclude-dir=build --exclude-dir=build_R* --exclude-dir=CMake --exclude-dir=build* "Edit[*:. \*\n;\+\=]{0,5}Axes" *

grep -ERIin "Edit[*:. \*\n;\+\=]{0,5}Axes" *

grep -ERIin "" *

scp -r lmlediae@visdemo.an.intel.com:/bertha/LLNL LLNL

tar -zxvf data.tar.gz

ssh vis-master.an.intel.com
vncserver (remote)
vncviewer (local)


./ospModelViewer --renderer pathtracer --max-depth 20 --hdri-light 0.5 "/home/lmlediae/git/po_fork/env_images/all_white_studio.pfm" "/home/lmlediae/Desktop/csafe_iso_modo1.obj"