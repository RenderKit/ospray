stages:
  - build
#  - deploy

variables:
  GIT_DEPTH: "3"

.job_template: &build_job
  type: build
  tags:
    - docker

### BUILD JOBS ###

build-arch-gcc:
  <<: *build_job
  image: jeffamstutz/docker-autobuild
  script:
    - scripts/build_gitlab/linux.sh

