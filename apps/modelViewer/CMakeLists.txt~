# --------------------------------------------
# include dependencies (glut, glut3dwidget, ...)
# --------------------------------------------
INCLUDE(${PROJECT_SOURCE_DIR}/cmake/glut.cmake)

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/ospray/include)

# --------------------------------------------
# main executable
# --------------------------------------------
ADD_EXECUTABLE(objView 
	# CPP sources
	objView.cpp
	objLoader.cpp
	)
TARGET_LINK_LIBRARIES(objView
	OSPRay
  ${OPENGL_LIBRARY} 
  ${GLUT_LIBRARY}
)
