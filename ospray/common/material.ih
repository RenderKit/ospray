#ifndef OSPRAY_MATERIAL_IH
#define OSPRAY_MATERIAL_IH

#include "ospray/common/ospcommon.ih"

/*! ISPC-side abstraction for a material. */
struct Material {
  void *uniform cppEquivalent; //! pointer back to the C++-equivalent of this class
};

#endif
